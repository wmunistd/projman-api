#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

DB_FILE=${PROJMAN_DB:-projman-test.db}

if [ -f "$DB_FILE" ]; then
  rm -f "$DB_FILE"
  echo "Removed $DB_FILE"
else
  echo "No test DB to remove ($DB_FILE)"
fi
