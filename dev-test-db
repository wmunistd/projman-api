#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

export PROJMAN_DB="projman-test.db"
export PROJMAN_PORT=${PROJMAN_PORT:-18080}
# Limpa o test DB anterior
./clear-test-db || true

# Inicia o servidor com auto-reload na porta de teste
./dev-server
